---
name : Installing apache server on the node Instance
hosts : all
become: true

tasks:
  - name : Installing apache server
    apt:
      name : apache2
      state : present

    notify:
      name : apache2
      state : started